# UX Revamp — SYS.HEALTH Dashboard — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Melhorar a UX do dashboard com sidebar pill ativo + smooth scroll, modais `@st.dialog` para todos os registros, e seções IA Coach / Biometria / Medicação ocultas por padrão atrás de toggle `⚙️ Avançado`.

**Architecture:** Três mudanças independentes em `dashboard.py` — injeção de CSS/JS para navegação, decoradores `@st.dialog` para formulários de entrada, e guards `if session_state.get("sidebar_avancado_open")` para seções secundárias. Nenhuma nova dependência de runtime. A ordem é Part 3 → Part 1 → Part 2 por risco crescente.

**Tech Stack:** Python, Streamlit ≥ 1.37 (necessário para `st.rerun(scope="fragment")` na Task 5), pg8000/SQLite via `db.py`, Supabase PostgreSQL em produção.

---

## Arquivos que serão modificados

| Arquivo | O quê muda |
|---|---|
| `dashboard.py` | Todas as 6 tasks — único arquivo da aplicação |
| `requirements.txt` | Bump `streamlit>=1.35.0` → `streamlit>=1.37.0` |

---

## Task 1: Ocultar seções IA Coach, Biometria e Medicação

**Arquivos:**
- Modify: `dashboard.py:1956-1963` (lista `_nav_items` no sidebar)
- Modify: `dashboard.py:3075-3264` (bloco Tirzepatida dentro de Registros do dia)
- Modify: `dashboard.py:3945-4415` (bloco IA Coach dentro de Histórico)
- Modify: `dashboard.py:4420-4735` (seção Biometria completa)

- [ ] **Step 1: Remover Biometria da lista de nav principal e adicionar toggle ⚙️ Avançado**

Em `dashboard.py`, localizar a lista `_nav_items` (~linha 1956) e substituir:

```python
    _nav_items = [
        ("sec-nutricao",   "🥗", "Nutrição"),
        ("sec-wearable",   "⌚", "Wearable · Agenda"),
        ("sec-evolucao",   "📈", "Evolução · Registros"),
        ("sec-banco",      "🍽️", "Banco de Refeições"),
        ("sec-historico",  "📊", "Histórico · Tendências"),
        ("sec-evacuacao",  "🚽", "Evacuação"),
    ]
```

(remove a linha `"sec-biometria"` da lista)

Logo após o `st.markdown(_nav_html, unsafe_allow_html=True)` que fecha o loop de nav (~linha 1984), adicionar o toggle ⚙️:

```python
    # ── Toggle Avançado ────────────────────────────────────────────────────────
    _av_open = st.session_state.get("sidebar_avancado_open", False)
    _av_lbl  = "▴" if _av_open else "▾"
    if st.button(f"⚙️  Avançado  {_av_lbl}", key="btn_avancado_toggle", use_container_width=True):
        st.session_state["sidebar_avancado_open"] = not _av_open
        st.rerun()
    if st.session_state.get("sidebar_avancado_open", False):
        _sec_av = [
            ("sec-ia",        "🤖", "IA Coach"),
            ("sec-biometria", "📏", "Biometria"),
            ("sec-medicacao", "💊", "Medicação"),
        ]
        _av_html = ""
        for _anc, _ic, _lb in _sec_av:
            _av_html += (
                f'<a href="#{_anc}" style="display:flex;align-items:center;gap:8px;'
                f'padding:5px 10px 5px 20px;border-radius:6px;text-decoration:none;'
                f'margin-bottom:2px;font-family:{MONO};font-size:10px;font-weight:600;'
                f'color:{MUTED};background:transparent;border:1px solid transparent" '
                f'onmouseover="this.style.color=\'{CYAN}\'" '
                f'onmouseout="this.style.color=\'{MUTED}\'">'
                f'<span style="font-size:11px">{_ic}</span><span>{_lb}</span></a>'
            )
        st.markdown(_av_html, unsafe_allow_html=True)
```

- [ ] **Step 2: Envolver o bloco Tirzepatida (Medicação) com guard**

Localizar o comentário `# ── Tirzepatida ───` (~linha 3081) e o separador visual logo acima (~linha 3075). Envolver o bloco completo da Tirzepatida (do separador até o final do bloco de edição de doses, ~linha 3264) com:

```python
    # ── Tirzepatida ───────────────────────────────────────────────────────────
    st.markdown('<div id="sec-medicacao"></div>', unsafe_allow_html=True)
    if st.session_state.get("sidebar_avancado_open", False):
        st.markdown(
            f'<div style="height:1px;background:{BORDER2};margin:14px 0 12px"></div>',
            unsafe_allow_html=True,
        )
        # ... (todo o código Tirzepatida existente aqui, indentado com 8 espaços) ...
```

(O separador visual e o anchor `#sec-medicacao` ficam fora do `if` para que o scroll funcione quando visível.)

- [ ] **Step 3: Envolver o bloco IA Coach com guard e adicionar anchor**

Localizar `# ── IA Coach ─────` (~linha 3945). Adicionar anchor e guard:

```python
    # ── IA Coach ─────────────────────────────────────────────────────────────
    st.markdown('<div id="sec-ia"></div>', unsafe_allow_html=True)
    if st.session_state.get("sidebar_avancado_open", False):
        st.markdown(sec("IA Coach", "Análise de Emagrecimento & Performance"), unsafe_allow_html=True)
        # ... (todo o código IA Coach existente aqui, indentado com 8 espaços) ...
```

- [ ] **Step 4: Envolver a seção Biometria inteira com guard**

Localizar `# SEÇÃO 5 — EVOLUÇÃO DE MEDIDAS` (~linha 4420). Envolver todo o bloco (do comentário SEÇÃO 5 até o `st.markdown('</div>')` final da biometria, ~linha 4735) com:

```python
if st.session_state.get("sidebar_avancado_open", False):
    # ════════════════════════════════════════════════════════════════════════════
    # SEÇÃO 5 — EVOLUÇÃO DE MEDIDAS (largura total — 11 colunas cabem melhor)
    # ════════════════════════════════════════════════════════════════════════════
    st.markdown('<div id="sec-biometria"></div>', unsafe_allow_html=True)
    # ... (todo o código Biometria existente aqui, indentado com 4 espaços) ...
```

- [ ] **Step 5: Validar sintaxe**

```bash
py -c "import ast; ast.parse(open('dashboard.py', encoding='utf-8').read()); print('OK')"
```

Esperado: `OK`

- [ ] **Step 6: Verificar comportamento no Streamlit Cloud**

Após push, verificar:
- `⚙️ Avançado ▾` aparece no sidebar abaixo dos links principais
- IA Coach, Biometria e Medicação NÃO aparecem na página por padrão
- Clicar `⚙️ Avançado` mostra os 3 sub-links e renderiza as 3 seções
- Clicar novamente esconde as 3 seções

- [ ] **Step 7: Commit**

```bash
git add dashboard.py
git commit -m "feat: ocultar IA Coach, Biometria e Medicação atrás de toggle Avançado no sidebar"
git push
```

---

## Task 2: Sidebar — smooth scroll + pill ativo

**Arquivos:**
- Modify: `dashboard.py` — início do bloco `with st.sidebar:` (~linha 1838)

- [ ] **Step 1: Injetar CSS + JS de smooth scroll e pill ativo no sidebar**

Localizar o bloco `with st.sidebar:` (~linha 1838) e adicionar como PRIMEIRA instrução dentro dele:

```python
with st.sidebar:
    st.markdown("""
<style>
/* Pill ativo no sidebar */
a.sh-nav-active {
    background: rgba(0,212,255,0.12) !important;
    border-color: rgba(0,212,255,0.35) !important;
    color: #00d4ff !important;
    border-left: 2px solid #00d4ff !important;
}
a.sh-nav-active span { color: #00d4ff !important; }
</style>
<script>
(function() {
  // Smooth scroll ao clicar em links #sec-*
  function initScrollLinks() {
    document.querySelectorAll('a[href^="#sec-"]').forEach(function(link) {
      link.addEventListener('click', function(e) {
        var target = document.querySelector(link.getAttribute('href'));
        if (target) {
          e.preventDefault();
          target.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      });
    });
  }

  // Pill ativo via IntersectionObserver
  function initActiveObserver() {
    var links = document.querySelectorAll('a[href^="#sec-"]');
    if (!links.length) return;
    var observer = new IntersectionObserver(function(entries) {
      entries.forEach(function(entry) {
        if (entry.isIntersecting) {
          var id = entry.target.id;
          links.forEach(function(l) { l.classList.remove('sh-nav-active'); });
          var active = document.querySelector('a[href="#' + id + '"]');
          if (active) active.classList.add('sh-nav-active');
        }
      });
    }, { threshold: 0.2 });
    document.querySelectorAll('div[id^="sec-"]').forEach(function(sec) {
      observer.observe(sec);
    });
  }

  // Aguarda DOM estabilizar após Streamlit renderizar
  setTimeout(function() { initScrollLinks(); initActiveObserver(); }, 800);
  // Re-inicializa quando Streamlit faz rerun (MutationObserver no body)
  new MutationObserver(function() {
    setTimeout(function() { initScrollLinks(); initActiveObserver(); }, 400);
  }).observe(document.body, { childList: true, subtree: true });
})();
</script>
""", unsafe_allow_html=True)
    # ... restante do sidebar ...
```

- [ ] **Step 2: Validar sintaxe**

```bash
py -c "import ast; ast.parse(open('dashboard.py', encoding='utf-8').read()); print('OK')"
```

Esperado: `OK`

- [ ] **Step 3: Verificar no Streamlit Cloud**

Após push, verificar:
- Clicar em "🥗 Nutrição" no sidebar faz scroll suave até a seção (sem salto)
- Ao scrollar manualmente, o link da seção visível fica com fundo ciano + borda esquerda
- Outros links voltam ao estado normal ao sair do viewport

- [ ] **Step 4: Commit**

```bash
git add dashboard.py
git commit -m "feat: smooth scroll e pill ativo no sidebar via IntersectionObserver"
git push
```

---

## Task 3: Modal `@st.dialog` para Água + HRV/PAI

**Arquivos:**
- Modify: `dashboard.py:1725` (função `_tab_agua`)
- Modify: `dashboard.py:1249` (função `_painel_entrada` — botão Água)
- Modify: `dashboard.py:1992-1997` (sidebar — atalho Água)

- [ ] **Step 1: Decorar `_tab_agua` com `@st.dialog`**

Localizar `def _tab_agua():` (~linha 1725) e adicionar o decorator:

```python
@st.dialog("💧 Água · HRV / PAI")
def _tab_agua():
    # ... conteúdo existente sem alterações ...
```

- [ ] **Step 2: Atualizar `_painel_entrada` — botão Água vira chamada direta**

Localizar em `_painel_entrada()` (~linha 1249) a linha do botão Água:

```python
    PAINEIS = [
        ("➕", "Refeição",    "refeicao"),
        ("💊", "Suplem.",     "suplemento"),
        ("💧", "Água",        "agua"),
        ("✏️", "Editar",      "editar"),
    ]
```

E o bloco de conteúdo (~linha 1270):

```python
    if atual is None:
        return
    with st.container(border=True):
        if atual == "refeicao":
            _tab_refeicao()
        elif atual == "suplemento":
            _tab_suplemento()
        elif atual == "agua":
            _tab_agua()         # ← agora chama o dialog diretamente
        elif atual == "editar":
            _tab_editar()
```

O botão "Água" na barra de `_painel_entrada` deve chamar `_tab_agua()` diretamente quando clicado. Alterar o loop de botões para detectar o clique em "agua" e chamar o dialog:

```python
    _nav_cols = st.columns(4)
    for i, (icon, label, key) in enumerate(PAINEIS):
        with _nav_cols[i]:
            ativo = (atual == key) if key != "agua" else False
            lbl   = f"{icon}  {label}" + ("  ▲" if ativo else "")
            if st.button(lbl, key=f"nav_{key}", use_container_width=True,
                         type="primary" if ativo else "secondary"):
                if key == "agua":
                    _tab_agua()   # abre modal direto
                else:
                    st.session_state["painel_aberto"] = None if ativo else key
                    st.rerun()
```

- [ ] **Step 3: Atualizar atalho rápido do sidebar para Água**

Localizar (~linha 1994):
```python
    if st.button("💧  Água", key="sb_btn_agua", use_container_width=True):
        st.session_state["painel_aberto"] = "agua"; st.rerun()
```

Substituir por:
```python
    if st.button("💧  Água", key="sb_btn_agua", use_container_width=True):
        _tab_agua()
```

- [ ] **Step 4: Validar sintaxe**

```bash
py -c "import ast; ast.parse(open('dashboard.py', encoding='utf-8').read()); print('OK')"
```

- [ ] **Step 5: Verificar no Streamlit Cloud**

Após push:
- Clicar "💧 Água" no top bar ou no sidebar abre um modal centralizado com fundo escuro
- Botões +200ml, +500ml, etc. registram água e fecham o modal
- Formulário HRV/PAI salva e fecha o modal
- O painel inline de água NÃO aparece mais na página

- [ ] **Step 6: Commit**

```bash
git add dashboard.py
git commit -m "feat: converter registro de Água para modal @st.dialog"
git push
```

---

## Task 4: Modal `@st.dialog` para Suplemento

**Arquivos:**
- Modify: `dashboard.py:1683` (função `_tab_suplemento`)
- Modify: `dashboard.py:1249` (função `_painel_entrada` — botão Suplem.)
- Modify: `dashboard.py:1996` (sidebar — atalho Suplemento)

- [ ] **Step 1: Decorar `_tab_suplemento` com `@st.dialog`**

Localizar `def _tab_suplemento():` (~linha 1683) e adicionar:

```python
@st.dialog("💊 Suplementação")
def _tab_suplemento():
    # ... conteúdo existente sem alterações ...
```

- [ ] **Step 2: Atualizar `_painel_entrada` — botão Suplem. vira chamada direta**

No loop de botões de `_painel_entrada`, aplicar o mesmo padrão da Task 3 para "suplemento":

```python
    _nav_cols = st.columns(4)
    for i, (icon, label, key) in enumerate(PAINEIS):
        with _nav_cols[i]:
            ativo = (atual == key) if key not in ("agua", "suplemento") else False
            lbl   = f"{icon}  {label}" + ("  ▲" if ativo else "")
            if st.button(lbl, key=f"nav_{key}", use_container_width=True,
                         type="primary" if ativo else "secondary"):
                if key == "agua":
                    _tab_agua()
                elif key == "suplemento":
                    _tab_suplemento()
                else:
                    st.session_state["painel_aberto"] = None if ativo else key
                    st.rerun()
```

- [ ] **Step 3: Atualizar atalho rápido do sidebar para Suplemento**

Localizar (~linha 1996):
```python
    if st.button("💊  Suplemento", key="sb_btn_supp", use_container_width=True):
        st.session_state["painel_aberto"] = "suplemento"; st.rerun()
```

Substituir por:
```python
    if st.button("💊  Suplemento", key="sb_btn_supp", use_container_width=True):
        _tab_suplemento()
```

- [ ] **Step 4: Validar sintaxe**

```bash
py -c "import ast; ast.parse(open('dashboard.py', encoding='utf-8').read()); print('OK')"
```

- [ ] **Step 5: Verificar no Streamlit Cloud**

Após push:
- Clicar "💊 Suplem." ou "💊 Suplemento" abre modal centralizado
- Marcar suplementos e salvar fecha o modal
- Painel inline de suplemento não aparece mais

- [ ] **Step 6: Commit**

```bash
git add dashboard.py
git commit -m "feat: converter registro de Suplemento para modal @st.dialog"
git push
```

---

## Task 5: Modal `@st.dialog` para Refeição

**Arquivos:**
- Modify: `requirements.txt` — bump para `streamlit>=1.37.0`
- Modify: `dashboard.py:1330` (função `_tab_refeicao`)
- Modify: `dashboard.py:1249` (função `_painel_entrada` — botão Refeição)
- Modify: `dashboard.py:1992` (sidebar — atalho Refeição)

- [ ] **Step 1: Atualizar requirements.txt**

Em `requirements.txt`, localizar a linha `streamlit>=1.35.0` e alterar para:

```
streamlit>=1.37.0
```

(Necessário para `st.rerun(scope="fragment")` usado nos steps seguintes.)

- [ ] **Step 2: Substituir `st.rerun()` de gestão de carrinho por `st.rerun(scope="fragment")`**

Dentro de `_tab_refeicao()` (~linha 1330), há vários `st.rerun()` usados para atualizar o carrinho após adicionar/remover itens. Estes devem usar `scope="fragment"` para não fechar o modal.

Localizar todos os `st.rerun()` DENTRO de `_tab_refeicao()` que são acionados por:
- Adicionar item ao carrinho (`carrinho_refeicao.append(...)`)
- Remover item do carrinho (`carrinho_refeicao.pop(...)`)
- Atualizar quantidade de item no carrinho

Substituir esses `st.rerun()` por `st.rerun(scope="fragment")`.

**NÃO substituir** o `st.rerun()` que ocorre APÓS salvar a refeição no banco (após o INSERT final) — esse deve continuar como `st.rerun()` para fechar o modal e atualizar o dashboard.

Linhas aproximadas para substituir (verificar contexto de cada uma):
- ~1316, ~1327, ~1410, ~1443, ~1515, ~1564, ~1568, ~1625, ~1629, ~1677, ~1681

Linhas para MANTER como `st.rerun()`:
- ~1722 (após limpar carrinho e salvar refeição)

- [ ] **Step 3: Decorar `_tab_refeicao` com `@st.dialog`**

```python
@st.dialog("➕ Nova Refeição", width="large")
def _tab_refeicao():
    # ... conteúdo com st.rerun(scope="fragment") para carrinho ...
```

- [ ] **Step 4: Atualizar `_painel_entrada` — botão Refeição vira chamada direta**

Atualizar o loop de botões para incluir "refeicao":

```python
    _nav_cols = st.columns(4)
    for i, (icon, label, key) in enumerate(PAINEIS):
        with _nav_cols[i]:
            ativo = (atual == key) if key not in ("agua", "suplemento", "refeicao") else False
            lbl   = f"{icon}  {label}" + ("  ▲" if ativo else "")
            if st.button(lbl, key=f"nav_{key}", use_container_width=True,
                         type="primary" if ativo else "secondary"):
                if key == "agua":
                    _tab_agua()
                elif key == "suplemento":
                    _tab_suplemento()
                elif key == "refeicao":
                    _tab_refeicao()
                else:
                    st.session_state["painel_aberto"] = None if ativo else key
                    st.rerun()
```

- [ ] **Step 5: Atualizar atalho rápido do sidebar para Refeição**

Localizar (~linha 1992):
```python
    if st.button("➕  Refeição", key="sb_btn_ref", use_container_width=True):
        st.session_state["painel_aberto"] = "refeicao"; st.rerun()
```

Substituir por:
```python
    if st.button("➕  Refeição", key="sb_btn_ref", use_container_width=True):
        _tab_refeicao()
```

- [ ] **Step 6: Validar sintaxe**

```bash
py -c "import ast; ast.parse(open('dashboard.py', encoding='utf-8').read()); print('OK')"
```

- [ ] **Step 7: Verificar no Streamlit Cloud**

Após push:
- Clicar "➕ Refeição" abre modal largo centralizado
- Buscar e adicionar alimentos ao carrinho sem fechar o modal
- Remover item do carrinho sem fechar o modal
- "Registrar refeição" salva e fecha o modal
- Upload de foto (Gemini) funciona dentro do modal

- [ ] **Step 8: Commit**

```bash
git add dashboard.py requirements.txt
git commit -m "feat: converter registro de Refeição para modal @st.dialog (width=large)"
git push
```

---

## Task 6: Modal `@st.dialog` para Evacuação

**Arquivos:**
- Modify: `dashboard.py:4806-4855` (botão + form de evacuação)

- [ ] **Step 1: Extrair form de evacuação para função `@st.dialog`**

Localizar o bloco de evacuação (~linha 4806). Extrair a lógica do formulário para uma função decorada, ANTES do bloco `_evac_open`:

```python
@st.dialog("🚽 Registrar Evacuação")
def _dialog_evacuacao():
    _ESF_OPTS = [
        "🟢 ■□□□□□  0 — Sem esforço · saiu sozinho, muito suave",
        "🟡 ■■□□□□  1 — Esforço normal · saiu sem machucar",
        "🟡 ■■■□□□  2 — Esforço leve+ · acima do normal, não machucou",
        "🟠 ■■■■□□  3 — Esforço grande · dificuldade para sair",
        "🔴 ■■■■■□  4 — Esforço forte · machucou e sangrou",
        "🔴 ■■■■■■  5 — Esforço máximo · não saia, ficou muito tempo no banheiro",
    ]
    with st.form("form_evac_modal", clear_on_submit=True):
        _ec1, _ec2 = st.columns(2)
        with _ec1:
            _evac_data = st.date_input(
                "Data", value=datetime.now(_BR).date(), key="evac_data_input"
            )
        with _ec2:
            _evac_hora = st.time_input(
                "Hora", value=datetime.now(_BR).time().replace(second=0, microsecond=0),
                key="evac_hora_input"
            )
        _evac_esforco_sel = st.selectbox(
            "Intensidade de esforço", _ESF_OPTS, index=0, key="evac_esforco_input"
        )
        _evac_obs = st.text_input(
            "Observação (opcional)", placeholder="Ex: consistência normal, dor abdominal…",
            key="evac_obs_input"
        )
        if st.form_submit_button("💾 SALVAR", use_container_width=True):
            _evac_dt = f"{_evac_data} {_evac_hora}"
            _evac_esforco_val = int(next(c for c in _evac_esforco_sel if c.isdigit()))
            DB.execute(
                "INSERT INTO evacuacoes (data_hora, esforco, observacao) VALUES (?, ?, ?)",
                [_evac_dt, _evac_esforco_val, _evac_obs.strip() or None]
            )
            st.cache_data.clear()
            _notif("Evacuação registrada ✓")
            st.rerun()
```

- [ ] **Step 2: Substituir botão toggle pelo botão que abre modal**

No local onde estava o botão toggle de evacuação (~linha 4806), substituir:

```python
# Antes (toggle):
# _evac_open = st.session_state.get("evac_nova_open", False)
# _evac_lbl  = "🚽 REGISTRAR EVACUAÇÃO ▴" if _evac_open else "🚽 REGISTRAR EVACUAÇÃO ▾"
# if st.button(_evac_lbl, key="btn_evac_nova", use_container_width=True):
#     st.session_state["evac_nova_open"] = not _evac_open
#     st.rerun()
# if st.session_state.get("evac_nova_open", False):
#     ... form inline ...

# Depois (modal):
if st.button("🚽 REGISTRAR EVACUAÇÃO", key="btn_evac_nova", use_container_width=True):
    _dialog_evacuacao()
```

- [ ] **Step 3: Remover o form inline que não é mais necessário**

Remover o bloco completo entre `if st.session_state.get("evac_nova_open", False):` e o `st.markdown('</div>', unsafe_allow_html=True)` que fechava esse bloco — esse código foi para `_dialog_evacuacao()`.

- [ ] **Step 4: Validar sintaxe**

```bash
py -c "import ast; ast.parse(open('dashboard.py', encoding='utf-8').read()); print('OK')"
```

- [ ] **Step 5: Verificar no Streamlit Cloud**

Após push:
- Clicar "🚽 REGISTRAR EVACUAÇÃO" abre modal centralizado
- Seletor de esforço com emojis e barra funciona dentro do modal
- Salvar fecha o modal e atualiza os cards de resumo
- O histórico de evacuações continua funcionando normalmente abaixo

- [ ] **Step 6: Commit**

```bash
git add dashboard.py
git commit -m "feat: converter registro de Evacuação para modal @st.dialog"
git push
```

---

## Self-Review

**Cobertura do spec:**
- ✅ Sidebar pill ativo + smooth scroll → Task 2
- ✅ @st.dialog para Água, Suplemento, Refeição, Evacuação → Tasks 3-6
- ✅ IA Coach oculto por padrão → Task 1, Step 3
- ✅ Biometria oculta por padrão → Task 1, Step 4
- ✅ Medicação oculta por padrão → Task 1, Step 2
- ✅ Sub-links ⚙️ Avançado com scroll → Task 1, Step 1
- ✅ Atalhos rápidos do sidebar atualizados → Tasks 3, 4, 5

**Sem placeholders:** todas as etapas têm código completo.

**Consistência de tipos:** `_tab_agua`, `_tab_suplemento`, `_tab_refeicao` decoradas com `@st.dialog` — chamadas diretas substituem `st.session_state["painel_aberto"]` em todos os pontos de trigger (top bar + sidebar).
